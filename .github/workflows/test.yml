name: Test Deno Debugger Skill

on:
  push:
    branches: [ main, develop, claude/* ]
  pull_request:
    branches: [ main, develop ]

jobs:
  unit-tests:
    name: Unit Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Cache pip packages
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements-dev.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-dev.txt

    - name: Run unit tests
      run: |
        pytest tests/test_breadcrumbs.py tests/test_heap_parser.py tests/test_cpu_parser.py -v --tb=short

  integration-tests:
    name: Integration Tests (Deno ${{ matrix.deno-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        deno-version: ["v1.x", "v2.x"]  # Test latest of each major version

    steps:
    - uses: actions/checkout@v4

    - name: Setup Deno
      uses: denoland/setup-deno@v1
      with:
        deno-version: ${{ matrix.deno-version }}

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r skill/requirements.txt

    - name: Create output directories
      run: |
        mkdir -p data output

    - name: Run validation tests
      run: |
        python validate.py
      timeout-minutes: 5
      continue-on-error: true  # Known issue: heap snapshots don't work with Deno

    - name: Upload validation artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: validation-artifacts-deno-${{ matrix.deno-version }}
        path: |
          data/validation_*.heapsnapshot
          data/validation_*.cpuprofile
          data/validation_*.json
          output/validation_*.png
          output/validation_*.org
        retention-days: 7

  lint:
    name: Code Quality
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install linting tools
      run: |
        pip install flake8 black isort

    - name: Check code formatting with black
      run: |
        black --check skill/scripts/ tests/ validate.py simulate_validation.py

    - name: Check import sorting with isort
      run: |
        isort --check-only skill/scripts/ tests/ validate.py simulate_validation.py

    - name: Lint with flake8
      run: |
        # Stop on errors, ignore line length (handled by black)
        # E402: module imports not at top (expected in test files that modify sys.path)
        # F541: f-string without placeholders (for print statements)
        flake8 skill/scripts/ tests/ validate.py simulate_validation.py --max-line-length=120 --extend-ignore=E203,W503,E402,F541

  type-check:
    name: Type Checking
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        pip install -r skill/requirements.txt
        pip install mypy

    - name: Type check with mypy
      run: |
        mypy skill/scripts/ --ignore-missing-imports --no-strict-optional
      continue-on-error: true  # Don't fail on type errors initially
