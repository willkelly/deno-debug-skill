{
  "tasks": {
    "test": "deno test --allow-read --allow-net --allow-write tests/",
    "lint": "deno lint tests/ && cd deno-debugger && deno lint",
    "fmt": "deno fmt tests/ && cd deno-debugger && deno fmt",
    "fmt:check": "deno fmt --check tests/ && cd deno-debugger && deno fmt --check"
  },
  "fmt": {
    "useTabs": false,
    "lineWidth": 100,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": false,
    "proseWrap": "preserve",
    "include": ["tests/"]
  },
  "lint": {
    "include": ["tests/"],
    "rules": {
      "tags": ["recommended"]
    }
  }
}
